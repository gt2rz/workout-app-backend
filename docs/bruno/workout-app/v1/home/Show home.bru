meta {
  name: Show home
  type: http
  seq: 2
}

get {
  url: {{base_url}}api/v1/home
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
