meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{base_url}}api/v1/auth/login
  body: json
  auth: bearer
}

body:json {
  {
    "email": "gt2rz.dev@gmail.com",
    "password": "12345678",
    "device":"bruno"
  }
}

vars:post-response {
  access_token: res.body.data.access_token
}

settings {
  encodeUrl: true
  timeout: 0
}
