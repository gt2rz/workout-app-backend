meta {
  name: Logout
  type: http
  seq: 3
}

post {
  url: {{base_url}}api/v1/auth/logout
  body: json
  auth: inherit
}

headers {
  X-API-KEY: default_api_key_value
}

settings {
  encodeUrl: true
  timeout: 0
}
